name: Pysa

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pysa:
    runs-on: ubuntu-latest

    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Run Pysa
        uses: facebook/pysa-action@v1
        
